name: Build Olauncher Import Fix

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Download previous fixed build
        run: |
          curl -L --fail 'https://sdmntprnorthcentralus.oaiusercontent.com/files/00000000-5430-822f-8e08-4dbb71d25cdf/raw?se=2026-08-06T16%3A16%3A02Z&sp=r&sv=2026-02-06&sr=b&scid=b08014ee-9424-5dd2-828b-5ac185b4e47b&skoid=38550de5-1fab-49d1-9ebb-83af5557cc43&sktid=a48cca56-e6da-484e-a814-9c849652bcb3&skt=2026-08-06T05%3A53%3A05Z&ske=2026-08-07T05%3A53%3A05Z&sks=b&skv=2026-02-06&sig=sTShTOA767t/wWAn7M5Xglbm7/mCKPbE3QNBfLtUiUE%3D' -o previous.zip
          mkdir previous repaired
          unzip -q previous.zip -d previous
          unzip -q previous/Olauncher-Font-Picker-v6.7.1-fixed-source.zip -d repaired

      - name: Repair font import flow
        run: |
          python3 - <<'PY'
          from pathlib import Path

          root = Path('repaired/olauncher')

          manager = root / 'app/src/main/java/app/olauncher/helper/FontManager.kt'
          manager_text = manager.read_text()
          old = '''        setSelected(context, option.id)\n        option\n'''
          new = '''        option\n'''
          if old not in manager_text:
              raise SystemExit('Could not find automatic font selection')
          manager.write_text(manager_text.replace(old, new, 1))

          settings = root / 'app/src/main/java/app/olauncher/ui/SettingsFragment.kt'
          settings_text = settings.read_text()
          old = '''            .onSuccess { option ->\n                requireContext().showToast(getString(R.string.font_added, option.name))\n                requireActivity().recreate()\n            }\n'''
          new = '''            .onSuccess { option ->\n                requireContext().showToast(getString(R.string.font_added, option.name))\n                binding.root.post {\n                    populateFontText()\n                    showFontPicker()\n                }\n            }\n'''
          if old not in settings_text:
              raise SystemExit('Could not find font import success callback')
          settings.write_text(settings_text.replace(old, new, 1))

          gradle = root / 'app/build.gradle'
          gradle_text = gradle.read_text()
          gradle_text = gradle_text.replace('versionName "v6.7.1-fonts1"', 'versionName "v6.7.1-fonts2"')
          gradle.write_text(gradle_text)
          PY

      - name: Build APK
        working-directory: repaired/olauncher
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon assembleDebug

      - name: Package build
        run: |
          cp repaired/olauncher/app/build/outputs/apk/debug/app-debug.apk Olauncher-Font-Picker-v6.7.1-import-fixed.apk
          (cd repaired && zip -qr ../Olauncher-Font-Picker-v6.7.1-import-fixed-source.zip olauncher)
          shasum -a 256 Olauncher-Font-Picker-v6.7.1-import-fixed.apk Olauncher-Font-Picker-v6.7.1-import-fixed-source.zip > Olauncher-Font-Picker-v6.7.1-import-fixed-SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: Olauncher-Font-Picker-v6.7.1-import-fixed
          path: |
            Olauncher-Font-Picker-v6.7.1-import-fixed.apk
            Olauncher-Font-Picker-v6.7.1-import-fixed-source.zip
            Olauncher-Font-Picker-v6.7.1-import-fixed-SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 7
